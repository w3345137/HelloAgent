# ── 运行时产物 ──
*.app/

# ── 依赖 ──
node_modules/

# ── 用户数据（运行时生成，不纳入版本控制）──
Data/
logs/
memory/
memorys/

# ── 会话与历史 ──
sessions/
sessions.json

# ── 版本补丁（运行时生成）──
src/patches/
src/versions/

# ── 环境与凭证 ──
.env
.env.local
*.key
*.pem
src/config.json
src/config/models.json

# ── IDE / 系统文件 ──
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# ── 临时文件 ──
.tmp-debug-session*/
test-*.js
_emoji_fix.py
00.md
*.log
*.bak
